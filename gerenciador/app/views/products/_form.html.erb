<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :cliente %>
    <%= collection_select(:client, :client_id, Client.all, :id, :nome)%>
  </div>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :product_nome %><br><br>
  </div>

  <div class="field">
    <%= form.label :quantidade %>
    <%= form.number_field :quantidade, id: :product_quantidade %><br><br>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
