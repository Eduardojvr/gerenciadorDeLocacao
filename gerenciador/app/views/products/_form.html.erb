<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
         <li><%= message %></li> 
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="field">
    <a>&nbsp &nbsp &nbsp</a><%= form.label :nome %>
    <center><%= form.text_field :nome, id: :product_nome %><br><br></center>
  </div>

  <div class="field">
    <a>&nbsp &nbsp &nbsp</a><%= form.label :quantidade %>
    <center><%= form.number_field :quantidade, id: :product_quantidade %><br><br>
  </div></center>

  <div class="actions">
     <center><button class="btn btn-lg btn-primary btn-block btn-signin" type="submit" >Enviar</button></center>

       
    <!-- <%= form.submit %> -->
  </div>
<% end %>
